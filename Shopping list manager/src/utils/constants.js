'use strict';

const AUTH_MODE = {
  isAdmin: 'isAdmin',
  isOwnerOrAdmin: 'isOwnerOrAdmin',
  getCurrentUser: 'getCurrentUser',
  hasAccess: 'hasAccess',
};

const ROLE = {
  admin: 'admin',
  user: 'user',
};

module.exports = {
  AUTH_MODE,
  ROLE,
};
